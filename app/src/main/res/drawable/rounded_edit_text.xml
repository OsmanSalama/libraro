<!-- res/drawable/rounded_edit_text.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="12dp"/> <!-- Adjust the radius to your preference -->
    <solid android:color="#4F504945"/> <!-- Background color -->
    <stroke
        android:width="0dp"
        android:color="@android:color/darker_gray"/> <!-- Optional: border color -->
</shape>
